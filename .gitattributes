# Normalise to LF in the repo. The images build on Linux and CI runs on Linux;
# CRLF in a shell script or entrypoint fails in confusing ways.
* text=auto eol=lf
*.png binary
*.jpg binary
*.webp binary
*.woff2 binary
