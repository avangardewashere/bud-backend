# dependencies
node_modules/
.pnp
.pnp.js

# build
dist/
build/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local
!.env.example

# logs
logs/
*.log
npm-debug.log*

# test / coverage
coverage/
.nyc_output/

# editors
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
.DS_Store
Thumbs.db

# runtime data
tmp/
uploads/

# prisma
prisma/*.db
prisma/*.db-journal
.neon

# Neon CLI output: production credentials, never committed
infra/neon/.env*

# Neon's vendored agent skills (`neon skills`). Regenerable, and mostly about
# Neon features Bud does not use, so they would read as noise in the repo.
.agents/
skills-lock.json
